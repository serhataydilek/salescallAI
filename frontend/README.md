# SalesMirror Frontend

Next.js frontend for the local-first SalesMirror MVP.

## Run locally

```powershell
cd frontend
npm install
npm run dev
```

Open `http://localhost:3000`.

Set `NEXT_PUBLIC_API_BASE_URL` if the backend is not running at `http://localhost:8000`.

